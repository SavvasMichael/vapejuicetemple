database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./data/samples;
  properties:
      charSet: UTF-8
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
      hibernate.hbm2ddl.auto: create

metrics:
  reporters:
    - type: log
      logger: metrics

